//Import fungsi dari file lain
const { namaFungsi, sayHello } = require("./fungsi");
const { car } = require("./property");


    //Panggil fungsinya
    sayHello("Nadiyah")
    //Import object and access object
    console.log(car.type)
    car.stop(car.type)
    car.start()

